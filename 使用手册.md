# 课题五成果展示平台本地使用手册

## 1. 项目位置

本地项目目录：

```text
H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements
```

项目主要包含：

```text
backend/        后端服务
frontend/       前端页面
mock-data/      模拟数据
docs/           接口及项目文档
```

本地运行时需要同时启动**后端服务**和**前端服务**。

---

## 2. 环境要求

建议使用：

```text
Node.js >= 22.12.0
npm >= 10
```

检查环境：

```powershell
node -v
npm -v
```

如果 PowerShell 无法执行 `npm`，可使用：

```powershell
npm.cmd -v
```

---

# 3. 启动后端

在 VS Code 中打开项目根目录：

```text
H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements
```

打开一个新的终端，执行：

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements"
```

启动后端：

```powershell
node backend/server.js
```

后端默认地址：

```text
http://localhost:3001
```

启动后不要关闭该终端。

### 后端接口测试

浏览器访问：

```text
http://localhost:3001/api/v1/llm/scenarios
```

如果返回类似：

```json
{
  "code": 200,
  "message": "success",
  "data": [],
  "timestamp": "..."
}
```

说明后端启动正常。

也可以测试：

```text
http://localhost:3001/api/v1/geodynamics/scenarios
http://localhost:3001/api/v1/automotive/scenarios
http://localhost:3001/api/v1/uav/scenarios
http://localhost:3001/api/v1/drug/scenarios
http://localhost:3001/api/v1/dft/scenarios
http://localhost:3001/api/v1/multicenter/clusters
```

---

# 4. 启动前端

在 VS Code 中再打开一个新的终端。

进入前端目录：

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements\frontend"
```

如果是第一次运行，需要先安装依赖：

```powershell
npm install
```

如果 PowerShell 无法执行 npm，可以使用：

```powershell
npm.cmd install
```

依赖安装完成后启动前端：

```powershell
npm run dev
```

或者：

```powershell
npm.cmd run dev
```

前端默认地址：

```text
http://localhost:3000
```

启动后不要关闭该终端。

---

# 5. 查看系统

浏览器打开：

```text
http://localhost:3000
```

即可进入课题五成果展示平台首页。

目前主要页面包括：

### 平台首页

```text
http://localhost:3000
```

### AI 大模型

```text
http://localhost:3000/domains/llm/scenarios
```

### 地球动力学

```text
http://localhost:3000/domains/geodynamics/scenarios
```

### 汽车结构仿真

```text
http://localhost:3000/domains/automotive/scenarios
```

### 无人机协同仿真

```text
http://localhost:3000/domains/uav/scenarios
```

### 虚拟药物筛选

```text
http://localhost:3000/domains/drug/scenarios
```

### 第一性原理材料计算

```text
http://localhost:3000/domains/dft/scenarios
```

### 多中心联调

```text
http://localhost:3000/multicenter
```

---

# 6. 检查前后端接口是否连通

前端已经配置 `/api/v1` 接口代理。

浏览器访问：

```text
http://localhost:3000/api/v1/llm/scenarios
```

如果能够正常返回 JSON 数据，则表示：

```text
浏览器
   ↓
前端 Nuxt（3000）
   ↓
/api/v1 接口代理
   ↓
后端 Node.js（3001）
   ↓
模拟数据
```

整个前后端链路已经正常。

因此可以通过两个地址分别检查：

后端直连：

```text
http://localhost:3001/api/v1/llm/scenarios
```

前端代理：

```text
http://localhost:3000/api/v1/llm/scenarios
```

两个地址都能正常返回数据，即表示接口联调正常。

---

# 7. 日常启动方式

以后每次开发或演示时，不需要重新执行 `npm install`。

只需要打开两个终端。

## 终端 1：后端

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements"

node backend/server.js
```

## 终端 2：前端

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements\frontend"

npm run dev
```

然后浏览器访问：

```text
http://localhost:3000
```

即可。

---

# 8. 更新 GitHub 代码后的使用方式

如果 GitHub 上有新的代码更新，先进入项目根目录：

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements"
```

执行：

```powershell
git pull origin main
```

如果本次更新修改了：

```text
frontend/package.json
frontend/package-lock.json
```

建议进入前端后重新执行：

```powershell
npm install
```

然后再：

```powershell
npm run dev
```

如果没有修改依赖，一般不需要重新安装。

---

# 9. 如何停止项目

前端或后端运行中的终端按：

```text
Ctrl + C
```

即可停止服务。

需要同时停止：

```text
前端 3000
后端 3001
```

---

# 10. 常见问题

## 10.1 前端打不开

检查前端终端是否仍然运行：

```powershell
npm run dev
```

并访问：

```text
http://localhost:3000
```

---

## 10.2 页面打开但没有数据

检查后端是否启动：

```powershell
node backend/server.js
```

再访问：

```text
http://localhost:3001/api/v1/llm/scenarios
```

---

## 10.3 前端接口报错或 502

通常说明：

```text
3000 前端正常
但
3001 后端没有启动
```

重新启动后端即可。

---

## 10.4 npm 无法执行

如果出现：

```text
禁止运行 npm.ps1
```

可以直接使用：

```powershell
npm.cmd install
npm.cmd run dev
```

---

## 10.5 npm 安装出现 Node 版本错误

确认：

```powershell
node -v
```

建议使用：

```text
Node.js 22.12.0 或更高版本
```

---

# 11. 当前系统说明

当前项目已经能够完成：

* 前端页面运行；
* 后端服务独立运行；
* 前端调用后端 API；
* 六大应用场景数据展示；
* 任务状态、工作流、指标、日志和结果信息展示；
* 四中心逻辑资源展示；
* 多中心函数联调相关数据展示。

当前计算结果及多中心资源数据主要用于**模拟联调和成果展示**，不应将其描述为真实异地超算运行数据。

---

# 12. 最简启动命令

日常使用时只需要记住：

**终端 1：**

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements"

node backend/server.js
```

**终端 2：**

```powershell
cd "H:\code\supercomputing_internet_topic5\Topic-Five-Exhibition-of-Supercomputing-Internet-Achievements\frontend"

npm run dev
```

**浏览器：**

```text
http://localhost:3000
```

**接口检查：**

```text
http://localhost:3000/api/v1/llm/scenarios
```

前端页面和该接口都能正常打开，即表示项目本地运行正常。
