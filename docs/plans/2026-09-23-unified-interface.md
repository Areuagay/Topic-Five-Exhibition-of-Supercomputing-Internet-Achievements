# 全站视觉统一与上传反馈

回退检查点：`34e2c82`。本轮直接在当前开发服务中实施，保留服务供体验。

设计：科研工作台采用纸白面板、冷灰画布、深灰文字与单一蓝色强调，去掉按钮的镜面高光与多层厚阴影。主色 #2563eb，文字 #243247，次级文字 #617086，边框 #dce3ec，画布 #f4f6f9，成功 #218264、移除 #c45555。正文沿用系统中文无衬线字体，数据使用等宽数字。细边框、8px 控件圆角、12px 面板圆角与一致的间距贯穿首页、六领域、运行记录、结果工作区、多中心。

- [x] 统一全局 tokens、Element Plus 与原生控件、卡片和表格，移除体验页的实体高光风格。
- [x] 将上传状态重构为固定尺寸控件，进度、完成、移除在同一空间转换；操作结果即时回填服务端数据。
- [x] 移除会推挤表格的反馈横幅；上传成功触发行级绿色扫光，移除触发红色扫光，错误与取消保留清晰文字反馈。
- [x] 浏览器测量上传/完成/移除全过程的行高与页面位置，验证绿色/红色动画、失败重试、取消、减少动态效果。
- [x] 检查首页、多中心、六领域、运行列表与结果页面的桌面和手机截图，运行类型检查、行为测试与独立生产构建。

使用现有开源 Element Plus、Motion 与 Lucide，不并行引入第二套组件主题。保留专业图表的数据语义颜色。动画只反馈交互，持续约 160–300ms；行级完成扫光约 900ms，支持中断和减少动态效果。

后续用户反馈：不要削去按钮动效；不再按 UI 设计技能执行。已恢复轻微悬停抬升、按压回弹和选中反馈，并把桌面导航改为侧栏，减少上方堆叠框架。行级扫光从浅色尾部递增至深色前沿，连续穿出右边界，取消末段停留；上传与移除使用独立动画名称，保证快速连续操作时重播。

验证：95 项 Node 测试、vue-tsc、独立生产构建通过；十二场景完整体验通过；最终侧栏布局下六领域动效与手机检查、故障恢复、上传重试取消、逐帧上传稳定性通过。前后端服务保持运行，前端改为独立后台进程。
