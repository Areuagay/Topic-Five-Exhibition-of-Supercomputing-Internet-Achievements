# 六领域体验完善

本轮以用户列出的优化点为范围，附件作为背景参考，不扩展到 P2 的函数服务、迁移或验收平台。

采用共享体验组件与统一模拟后端，保留现有六步导航、蓝白风格和专业结果工作区。上传按数据大小显示 1.5–12 秒的模拟传输进度，支持取消和重试；内置数据不显示无效删除按钮。六领域十二场景均可上传、导入和提交新任务。

运行以服务端时间推进，列表、工作流与详情共用状态；多次刷新或多窗口不会加速任务。总进度由节点平均值计算，失败与停止任务不推进。成功且有结果的记录才提供结果入口，其他记录给出明确状态。模拟运行状态保存至忽略目录，不改写版本控制中的演示种子。

- [x] 补充覆盖六领域、十二场景、上传重置、时间推进和结果可用性的行为测试，先复现缺陷。
- [x] 将领域专属写接口替换为共享模拟存储，修正第二场景复用错误工作流的问题。
- [x] 优化数据准备按钮、上传反馈、导入状态和错误反馈。
- [x] 开放六领域提交与轮询，修正重复提交、缓存结果及运行状态不一致。
- [x] 运行已有测试、类型检查、构建和浏览器交互验收，记录限制与截图。

验证结果：95 项 Node 测试通过，vue-tsc 通过，独立 buildDir 生产构建通过。十二场景浏览器流程全部通过；故障恢复、延迟响应竞态、上传取消与重试、390px 页面检查通过。前端 3000 与后端 3001 保持运行。

追加视觉与动效：使用 Motion 和 Lucide 统一六领域按钮质感、选择反馈、步骤定向过渡及上传状态切换。算子卡片、勾选标记、已选数量与标签联动，快速操作可中断动画，标签区域平滑展开收起。保留监控状态色，加深数据表关键信息文字；修复冷启动时事件绑定前的首击丢失。

追加验证：95 项 Node 测试、vue-tsc、独立生产构建再次通过；六领域选择/筛选交错、快速切步、手机布局和减少动态效果检查通过，上传与异常恢复脚本再次通过。浏览器检查同时覆盖可见数据表文字对比度及监控语义颜色未被渐变覆盖。服务继续保留。
